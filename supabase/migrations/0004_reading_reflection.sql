-- ============================================================================
-- הוספת שדה לשלב "סיכום" במשימת הקריאה (סעיף 10 במפרט): התלמיד מסביר במילים
-- שלו מה למד. זהו טקסט חופשי לכל מפגש קריאה, לא קשור לשאלה ספציפית,
-- ולכן אינו שייך לטבלת answers.
-- ============================================================================

alter table public.reading_sessions
  add column reflection_text text;
