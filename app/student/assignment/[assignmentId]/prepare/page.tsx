import { ComingSoon } from "@/components/shared/coming-soon";

export const metadata = { title: "משימת קריאה — ReadWise AI" };

export default function AssignmentPreparePage() {
  return (
    <ComingSoon
      title="משימת קריאה"
      description="חמשת שלבי משימת הקריאה (התכוננות, קריאה, הבנת הטקסט, אתגר חשיבה, סיכום) ייבנו בשלב ה-Question Engine."
    />
  );
}
